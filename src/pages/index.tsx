import React, { FC } from "react";
import Layout from "../components/Layout/Layout";

const IndexPage: FC = () => {
    return (
        <Layout>
            <h1>Hello world</h1>
        </Layout>
    );
};

export default IndexPage;
