import React, { FC } from "react";
import { FooterWrapper } from "./Footer.css";

const Footer: FC = () => (
    <FooterWrapper>
        <div>Footer</div>
    </FooterWrapper>
);

export default Footer;
